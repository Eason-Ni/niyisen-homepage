# 2026-05-24 Songyue Reference Homepage Refresh

## 目标

- 把松月个人网站作为本项目的内部参考对象，沉淀到 `docs/research/`。
- 借鉴其有效的信息架构、视觉节奏和商业化表达方式，优化 Eason 个人主页。
- 参考拆解不进入站点导航、不在页面上展示。

## 范围

- 新增内部研究文档。
- 更新静态页面 `index.html`、`styles.css`、`script.js`。
- 保留中英文切换、GitHub 入口、公众号二维码和静态部署方式。

## 非范围

- 不发布、不 push、不改 GitHub Pages 设置。
- 不复制松月站点的个人照片、品牌文案、商品链接或商业背书。
- 不新增构建链路、框架、数据库或外部服务依赖。

## 已验证事实

- 参考站 URL：`https://silver-chebakia-3ff633.netlify.app/`。
- 参考站是单页个人网站，包含 hero、过往、文章、书和播客、品牌合作、付费产品、联系。
- 当前项目是纯静态站点，入口文件为 `index.html`、`styles.css`、`script.js`。
- 当前本地预览可通过 `python3 -m http.server 8123` 运行。

## 执行步骤

1. 抽取参考站内容结构、视觉系统、信息架构和可借鉴原则。
2. 写入内部 reference 文档。
3. 改造主页结构和视觉表达。
4. 本地启动静态服务，检查桌面和移动首屏、语言切换、链接和控制台。

## 风险和停止条件

- 如果参考站无法访问，不使用相似网站替代其正文和结构。
- 如果本地页面出现空白、明显错位、语言切换失效或图片加载失败，先修复再汇报。
- 如果发现敏感信息或本机路径将进入公开页面，立即停止并移除。

## 验证方式

- 浏览器打开 `http://localhost:8123/`。
- 检查桌面 `1280x720` 和移动 `390x844`。
- 检查中英文切换、CTA 链接、GitHub 链接、二维码图片。
- 检查控制台 error/warn。
- 执行基础安全扫描，避免本机路径和密钥进入公开页面。

## 回滚方式

- 静态文件改动集中在 `index.html`、`styles.css`、`script.js` 和新增文档；可通过 Git diff 精确回滚。
