# 个人主页优化 TODO

## 当前现状

- 项目位置：当前仓库根目录
- 形式：纯静态个人主页，包含 `index.html`、`styles.css`、`script.js`、`favicon.svg`、`CNAME`
- Git 状态：已连接远程仓库 `https://github.com/Eason-Ni/niyisen-homepage.git`
- 域名状态：`https://niyisen.com` 当前可访问，返回 GitHub Pages 页面
- 本地预览：已用 `python3 -m http.server 8080` 跑通
- 页面检查：桌面和手机首屏均能正常渲染，未见明显错位或空白
- 已有资料：`docs/research/github-pages-opportunities.md` 已整理 GitHub Pages 能力与主页扩展方向，但当前还未纳入 Git
- 2026-04-26 更新：主页已支持英文/中文切换，默认英文；联系区已加入公众号二维码；README 已同步当前域名状态；已补 `robots.txt` 与 `sitemap.xml`

## P0：先修会影响上线判断的问题

1. [已完成] 同步 README 的域名说明
   - 现状：README 还写着“DNS 准备好前不要加 CNAME”，但仓库里已经有 `CNAME=niyisen.com`，而且域名已能访问。
   - 目标：把 README 改成当前真实状态：已使用 GitHub Pages + 自定义域名。

2. 确认 GitHub Pages 自定义域名配置
   - 现状：`https://niyisen.com` 可访问，`https://eason-ni.github.io/niyisen-homepage/` 会跳转到自定义域名。
   - 目标：在 GitHub Pages 设置里确认是否启用了 HTTPS 强制跳转，并记录到 README。

3. [已完成] 补一个上线检查清单
   - 目标：把“本地预览、桌面截图、手机截图、线上域名、GitHub Pages 跳转”写成固定检查项，避免以后改页面只靠肉眼猜。

4. [已完成] 基于松月个人网站做内部参考拆解并优化主页
   - 目标：把参考站拆解沉淀到 `docs/research/`，但不接入公开页面；同时优化 Eason 主页首屏、内容结构和联系路径。
   - 验证：本地预览桌面和移动端、语言切换、链接、控制台和基础敏感信息扫描。
   - 产物：`docs/research/songyue-personal-site-reference-2026-05-24.md`、`docs/specs/2026-05-24-songyue-reference-homepage-refresh.md`。

## P1：让主页从“骨架”变成“能介绍你”的页面

1. [已完成] 补个人定位的中文版本
   - 现状：页面主体是英文，公众号是中文，受众不够统一。
   - 建议：保留英文质感，但增加一段简洁中文介绍，讲清楚你是谁、现在关注什么、别人为什么要找你。

2. [已完成初版] 增加精选项目入口
   - 现状：Work 只有三个方向，没有具体成果。
   - 建议：至少放 3 个项目或长期主题，例如 AI 搜索推荐、内容理解、论文研究、知识库、工具/Skill 工作台。

3. [已完成初版] 增加“可联系入口”
   - 现状：只有 GitHub 和公众号名，用户还需要自己搜索。
   - 建议：补充邮箱、公众号二维码图片，或者一个明确的联系说明。

4. [已完成初版] 增加研究/文章入口
   - 现状：主页没有承接后续内容的入口。
   - 建议：先加一个 `Notes` 或 `Writing` 区块，即使暂时只放 2-3 个占位主题，也比空着强。

## P2：提升传播和搜索效果

1. 补社交分享信息
   - 目标：增加页面标题、摘要、分享图信息，确保发到微信、飞书、X、LinkedIn 时不是默认空白卡片。

2. [已完成] 增加 `robots.txt` 和 `sitemap.xml`
   - 目标：让搜索引擎更容易收录。

3. 准备一张正式头像或主页主视觉
   - 现状：目前用抽象动画表达 AI 搜索推荐，辨识度不错，但缺少“人”的信号。
   - 建议：可以加入头像、个人照片，或更有个人记忆点的主视觉。

4. 优化 favicon
   - 现状：已有 `favicon.svg`。
   - 建议：补充 PNG/Apple touch icon，兼容手机收藏、浏览器标签和部分旧环境。

## P3：体验和可维护性

1. [已完成初版] 减少动画对低性能设备的影响
   - 现状：页面支持减少动效偏好，但 canvas 动画仍会持续运行。
   - 目标：当用户系统开启减少动效时，停止或弱化动画。

2. 增加基本无障碍检查
   - 目标：确认键盘导航、链接文字、颜色对比度、移动端字号都可用。

3. 建立截图留档
   - 目标：保留桌面和手机截图，后续改版时可以对比首屏是否变差。

4. 拆出内容配置
   - 现状：所有文案都写在 `index.html` 里。
   - 建议：等内容变多后，再把项目、文章、链接整理成统一配置，避免每次改 HTML。

## 建议执行顺序

1. 先改 README 和上线检查清单。
2. 再补中文个人介绍、精选项目、联系入口。
3. 然后补分享信息、搜索收录文件、截图留档。
4. 最后再处理动画、无障碍和内容配置。
